version: "3.9"

services:
  tbai:
    build: .
    container_name: tbai-v3
    ports:
      - "7860:7860"

    volumes:
      - ./models:/app/models
      - ./Cases:/app/Cases

    environment:
      GRADIO_SERVER_NAME: 0.0.0.0
      GRADIO_SERVER_PORT: 7860
      PYTHONUNBUFFERED: "1"

    restart: unless-stopped
